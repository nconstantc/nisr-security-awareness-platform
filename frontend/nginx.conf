server {
    listen 80;
    server_name _;
    client_max_body_size 10m;
    server_tokens off;

    root /usr/share/nginx/html;
    index index.html;

    # $http_host (not $host) - $host strips the port, which broke absolute media URLs
    # (request.build_absolute_uri()) on any non-default port like the local :8090 dev setup.
    location /api/ {
        proxy_pass http://awareness-main-backend-1:8000/api/;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /admin/ {
        proxy_pass http://awareness-main-backend-1:8000/admin/;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ckeditor5/ {
        proxy_pass http://awareness-main-backend-1:8000/ckeditor5/;
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /usr/share/nginx/django-static/;
    }

    location /media/ {
        alias /usr/share/nginx/django-media/;
    }

    location / {
        try_files $uri /index.html;
    }
}